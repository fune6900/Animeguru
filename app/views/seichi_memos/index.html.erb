<h2 class="text-2xl font-bold text-center mb-6">みんなの投稿</h2>
<div class="max-w-full mx-auto space-y-4">
  <% if @seichi_memos.any? %>
    <% @seichi_memos.each do |seichi_memo| %>
      <%= render "seichi_memos/seichi_memo", seichi_memo: seichi_memo %>
    <% end %>
  <% else %>
    <p class="text-center text-gray-600">聖地メモがありません。</p>
  <% end %>
</div>
