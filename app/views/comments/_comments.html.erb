<div id="comments" class="space-y-4 mb-4 w-full max-w-2xl mx-auto px-4">
  <% if comments.any? %>
    <% comments.each do |comment| %>
      <%= render 'comments/comment', comment: comment %>
    <% end %>
  <% end %>
</div>
